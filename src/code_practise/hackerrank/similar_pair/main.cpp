#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <bitset>

using namespace std;
bitset<100001> ps;


int main() {
    int n, T; cin >> n >> T;
    vector<vector<int>> tree(n);
    ps.reset();
    for(int i = 0; i < )

    return 0;
}

